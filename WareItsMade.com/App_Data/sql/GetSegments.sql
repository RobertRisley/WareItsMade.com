SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE GetSegments 
AS
BEGIN
	SET NOCOUNT ON;
	SET FMTONLY OFF;

	SELECT DISTINCT
		SegmentId
	,	SegmentTitle
	FROM dbo.Unspsc
	ORDER BY SegmentTitle
END
GO
